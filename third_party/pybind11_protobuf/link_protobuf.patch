diff --git a/pybind11_protobuf/BUILD b/pybind11_protobuf/BUILD
index d9abcaa..e7ee8cd 100755
--- a/pybind11_protobuf/BUILD
+++ b/pybind11_protobuf/BUILD
@@ -1,7 +1,7 @@
 # Pybind11 bindings for Google's Protocol Buffers

 load("@pybind11_bazel//:build_defs.bzl", "pybind_library")
-
+exports_files(["proto_cast_util.cc"])
 licenses(["notice"])

 pybind_library(
@@ -29,15 +29,48 @@ pybind_library(
         "@com_google_protobuf//:protobuf",
     ],
 )
+'''
+cc_library(
+    name = "proto_cast_util_cc",
+    srcs = ["proto_cast_util.cc"],
+    hdrs = [
+        "proto_cast_util.h",
+        "proto_caster_impl.h",
+    ],
+    alwayslink = 1,
+    visibility = ["//visibility:public"],
+    copts = ["-fvisibility=default"],
+    deps = [
+        "@pybind11",
+        ":check_unknown_fields",
+        "@com_google_absl//absl/container:flat_hash_map",
+        "@com_google_absl//absl/log",
+        "@com_google_absl//absl/log:check",
+        "@com_google_absl//absl/memory",
+        "@com_google_absl//absl/strings",
+        "@com_google_absl//absl/types:optional",
+        "@com_google_protobuf//:protobuf",
+        "@com_google_protobuf//python:proto_api",
+    ],
+)
+'''

-pybind_library(
+cc_library(
     name = "proto_cast_util",
     srcs = ["proto_cast_util.cc"],
+    visibility = [
+        "//visibility:public",
+    ],
     hdrs = [
         "proto_cast_util.h",
         "proto_caster_impl.h",
     ],
+    alwayslink = 1,
+    copts = [
+        "-fvisibility=default",
+    ],
     deps = [
+        "@pybind11",
         ":check_unknown_fields",
         "@com_google_absl//absl/container:flat_hash_map",
         "@com_google_absl//absl/log",
@@ -90,3 +123,4 @@ cc_library(
     ],
     alwayslink = 1,
 )
+
diff --git a/pybind11_protobuf/proto_cast_util.cc b/pybind11_protobuf/proto_cast_util.cc
index 5ca857b..96c915c 100755
--- a/pybind11_protobuf/proto_cast_util.cc
+++ b/pybind11_protobuf/proto_cast_util.cc
@@ -1,3 +1,4 @@
+#pragma GCC visibility push(default)
 #include "pybind11_protobuf/proto_cast_util.h"

 #include <Python.h>
@@ -624,3 +625,4 @@ py::handle GenericProtoCast(Message* src, py::return_value_policy policy,
 }

 }  // namespace pybind11_protobuf
+#pragma GCC visibility pop
